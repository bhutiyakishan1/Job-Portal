/**
 * @license Copyright (c) 2013, CKSource - Frederico Knabben
 * For licensing, see LICENSE
 */

"use strict";

/* global describe: false, it: false */

var assert = require("assert"),
    dataContainer = require(global.MODULE_ROOT + "/libraries/modules/data-container");

describe("data-container", function () {
    it("exports data containers", function () {
        assert.ok(dataContainer.LinkedList);
        assert.ok(dataContainer.Map);
        assert.ok(dataContainer.Repository);
    });
});
